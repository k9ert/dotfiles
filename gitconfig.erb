[user]
	name = <%= print("Your Name: "); STDOUT.flush; @name or STDIN.gets.chomp %>
	email = <%= print("Your Email: "); STDOUT.flush; @email or STDIN.gets.chomp %>
[alias]
	co = checkout
[color]
	diff = auto
	status = auto
	branch = auto
[core]
	excludesfile = <%= ENV['HOME'] %>/.gitignore
	editor = mate -w
	autocrlf = input
[apply]
	whitespace = nowarn
[format]
	pretty = %C(yellow)%h%Creset %s %C(red)(%an, %cr)%Creset
[github]
	user = <%= print("GitHub Username: "); STDOUT.flush; @ghusername or STDIN.gets.chomp %>
